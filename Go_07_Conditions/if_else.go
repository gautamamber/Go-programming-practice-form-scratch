// main package
package main 

// Package fmt implements formatted I/OPackage fmt implements formatted I/O
import "fmt"

// main function must, run automatically
func main() {

	a := 2
	if a > 10 {
		fmt.Println("Yes a is geater")
	} else {
		fmt.Println("No a is not geater")
	}
		
}