// main package
package main 

// Package fmt implements formatted I/OPackage fmt implements formatted I/O
import ("fmt"
		"math")

// main function must, run automatically
func main() {
		
	fmt.Println(math.Floor(22.2))
	fmt.Println(math.Sqrt(64))
	fmt.Println(math.Ceil(22.2))
	fmt.Println(math.Abs(22.2))
}
